# Next Enhancements — Rolling Task List

**Active tasks:** none.

The current build is planned in `./next-implementation.md` (phased, derived from the
backend branch's Source-of-Truth artifacts) rather than as rolling enhancement tasks.

Typing `n` / `next` with no `[TODO]` items here triggers the "e" workflow in
`../AGENTS.md` §1, which regenerates this list with exactly 3 tasks per application
section. Do that rather than hand-editing a partial list.

## Sections a regenerated plan must cover

- Monitoring CCTV (`/`)
- Peta Gate (`/map`)
- Buku Lintasan (`/ledger`)
- Pemeriksa Lintasan (`/crossing`)
- Riwayat Pembacaan (`/cctv-history`)
- Daftar Nomor Lambung (`/fleet`)
- Laporan Harian & Shift (`/reports`)
- Konfigurasi Sistem (`/settings`)
- Perangkat Edge (`/settings/devices`)
- Tayangan Langsung Gate (`/live/[camera_code]`)
